#Config file for editiing all the checks. :)
#Placeholders: %player% %check% %sub-check% %name% %vl% %ping% %tps% %world% %x% %y% %z%
#Suggest more placeholders please.
#vl-add is how much vl the subcheck should add.

checks:
  killaura:
    sub-check:
      type-a:
        vl-add: 4
      type-b:
        vl-add: 2
      type-c:
        vl-add: 3
      type-d:
        vl-add: 8
      type-e:
        vl-add: 6
      type-f:
        vl-add: 8
      type-g:
        vl-add: 7
    actions:
     25*2: cancel
     50*2: 'vortex notify %player% in %world% at %x%,%y%,%z% might be using %check%. Detected with %sub-check%. Vl: %vl% Ping: %ping% Tps: %tps%'
     200: 'vortex kick %plater% Hacking.'
  reach:
    sub-check:
      type-a:
        vl-add: 4
      type-b:
        vl-add: 8
    actions:
      8*2: cancel
      16*2: 'vortex notify %player% in %world% at %x%,%y%,%z% might be using %check%. Detected with %sub-check%. Vl: %vl% Ping: %ping% Tps: %tps%'
      50: 'vortex kick %player% Hacking.'
  speed:
    sub-check:
      type-a:
        vl-add: 5
    actions:
      20*2 cancel
      60*2: 'vortex notify %player% in %world% at %x%,%y%,%z% might be using %check%. Detected with %sub-check%. Vl: %vl% Ping: %ping% Tps: %tps%'
      100: 'vortex kick %player% Hacking.'
  fly:
    sub-check:
      type-a:
        vl-add: 10
    actions:
      40*2: cancel
      100*2: 'vortex notify %player% in %world% at %x%,%y%,%z% might be using %check%. Detected with %sub-check%. Vl: %vl% Ping: %ping% Tps: %tps%'
      400: 'vortex kick %player% Hacking.'
  nofall:
    sub-check:
      type-a:
        vl-add: 5
      type-b:
        vl-add: 5
    actions:
      5:cancel
      20*2: 'vortex notify %player% in %world% at %x%,%y%,%z% might be using %check%. Detected with %sub-check%. Vl: %vl% Ping: %ping% Tps: %tps%'
      50: 'vortex kick %player% Hacking.'
